import { Button } from "../common/Button";

export const PaginationGap = () => {
  return (
    <Button className="ring-0 hover:bg-gray-100 dark:hover:bg-blue-800" disabled type="button">
      ...
    </Button>
  );
};
